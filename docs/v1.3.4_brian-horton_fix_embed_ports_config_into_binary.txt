Version: 1.3.4
Type: fix
Author: brian-horton
Date: 2026-03-19

Issues:
  - Port config (hostname, port settings) not persistent in --basic QEMU mode
  - backend/ports.json only accessible via VirtioFS, not in basic mode
  - Users had no way to set a persistent hostname without VirtioFS

Changes:
  - backend/ports.json is now compiled into the binary via include_str!()
  - Edit backend/ports.json → cargo build → config is baked in, no VirtioFS needed
  - Load priority: Storage KV → VirtioFS /backend/ports.json → embedded (compiled-in)
  - This matches how TLS certs work (embedded fallback + runtime override)

Files Changed:
  - src/ports.rs (added EMBEDDED_CONFIG via include_str!, updated load order)
  - version.json (1.3.3 -> 1.3.4)
  - Cargo.toml (1.3.3 -> 1.3.4)

Build Status: PASS — compiles for x86_64-unknown-hermit with zero errors
